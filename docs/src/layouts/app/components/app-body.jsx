import React, { useContext } from 'react';
import classNames from 'classnames';

const AppBody = ({ children }) => {
  return <div className="App-body AppBody">{children}</div>;
};

export default AppBody;
